{
  "name": "@rtf-dm/artnet-packets",
  "version": "0.0.1",
  "description": "ArtNet packets package for protocol version 14",
  "main": "build/index.js",
  "types": "build/types",
  "devDependencies": {},
  "scripts": {
    "build": "tsc --build",
    "cleanup": "tsc --build --clean"
  },
  "author": "Rize-The-Flag",
  "license": "MIT",
  "dependencies": {
    "@rtf-dm/protocol": "^0.2.0"
  },
  "files": [
    "build"
  ]
}
